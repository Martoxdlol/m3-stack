// import { Hono } from "hono";
// // import { serveStatic } from "@hono/node-server/serve-static";

// export type CreateServerOptions<T extends Hono> = {
//   api: T;
// };

// export function createServer<T extends Hono>(opts: CreateServerOptions<T>) {
//   const app = new Hono().route("/api", opts.api);
// }
